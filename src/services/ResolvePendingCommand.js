// @ts-check

class ResolvePendingCommand {
    /**
     * 
     * @param {number} usId 
     * @param {number} themId 
     */
    constructor(usId, themId) {
        this.usId = usId;
        this.themId = themId;
    }
}

module.exports = ResolvePendingCommand